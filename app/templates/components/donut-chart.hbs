<svg height="160" width="160" viewBox="0 0 160 160">
  {{#each @segments key="@index" as | segment index |}}
    {{#if (gt segment.count 0)}}
      <DonutSegment @index={{index}} @fractions={{this.fractions}} />
    {{/if}}
  {{/each}}
</svg>
